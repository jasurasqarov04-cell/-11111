# Скопируйте в .env на сервере (или локально) и подставьте токен от @BotFather
BOT_TOKEN=

# Необязательно: на Render, если нет RENDER_EXTERNAL_URL — публичный URL сервиса для keepalive (раз в ~14 мин)
# KEEPALIVE_URL=https://ваш-сервис.onrender.com
# KEEPALIVE_INTERVAL_SEC=840
